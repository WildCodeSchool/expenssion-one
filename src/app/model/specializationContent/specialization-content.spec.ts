import { SpecializationContent } from './specialization-content';

describe('SpecializationContent', () => {
  it('should create an instance', () => {
    expect(new SpecializationContent()).toBeTruthy();
  });
});
